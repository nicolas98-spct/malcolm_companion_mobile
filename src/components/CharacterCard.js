import { Image, Pressable, Text, View } from 'react-native';
import { impact } from '../utils/haptics';
export default function CharacterCard({ character, onPress }) { return <Pressable className="bg-white rounded-3xl p-3 mb-3 flex-row items-center" style={{ backgroundColor: '#fff', borderRadius: 24, padding: 12, marginBottom: 12, flexDirection: 'row', alignItems: 'center' }} onPress={() => { impact(); onPress?.(); }}><Image source={character.image} style={{ width: 68, height: 68, borderRadius: 34, backgroundColor: '#ddd' }} /><View style={{ flex: 1, marginLeft: 14 }}><Text style={{ fontSize: 19, fontWeight: '900' }}>{character.name}</Text><Text numberOfLines={2} style={{ color: '#6b7280', marginTop: 3 }}>{character.description}</Text></View><Text style={{ fontSize: 26, color: '#d71920' }}>›</Text></Pressable>; }
