import { Image, Pressable, Text, View } from 'react-native';
import { impact } from '../utils/haptics';
export default function EpisodeCard({ episode, onPress }) { return <Pressable className="bg-black rounded-3xl p-3 mb-4" style={{ backgroundColor: '#101010', borderRadius: 24, padding: 12, marginBottom: 16 }} onPress={() => { impact(); onPress?.(); }}><Image source={episode.image} style={{ width: '100%', height: 150, borderRadius: 18 }} /><View style={{ padding: 10 }}><Text style={{ color: '#fff', fontSize: 22, fontWeight: '900' }}>{episode.title}</Text><Text numberOfLines={2} style={{ color: '#cfcfcf', marginTop: 5 }}>{episode.description}</Text><Text style={{ color: '#ffc928', marginTop: 8, fontWeight: '900' }}>★ {episode.rating} · Temporada {episode.season}</Text></View></Pressable>; }
